SELECT TRUNCATE(SUM(LAT_N),4)
FROM station
WHERE (LAT_N>38.7880) AND (LAT_N<137.2345);
TRUNCATE() means to display number after decimal point however round() function round off the last digit after decimal point
